# FastAPI service layer
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0

# Vendored job-agent-real dependencies (kept in sync with
# job_agent_real/requirements.txt)
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
curl_cffi>=0.15.0
firecrawl-py>=1.0.0
python-dotenv>=1.0.0
playwright==1.60.0   # pinned to match the mcr.microsoft.com/playwright base image tag
